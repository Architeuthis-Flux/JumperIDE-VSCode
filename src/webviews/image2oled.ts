/**
 * Image-to-OLED converter webview — stub for future implementation.
 * Will port JumperIDE's image2oled.html into a VSCode webview panel.
 */

import * as vscode from 'vscode';

export function registerImage2Oled(context: vscode.ExtensionContext): void {
    // Future: register command jumperless.openImage2Oled
    // that opens a webview panel with the image conversion UI
}
